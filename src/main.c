#include "../include/mem_metrics.h" // Include the header for memory metrics functionality.

int main (void) {
    efficiency_test_all_methods(); // Run the efficiency test for all allocation methods.
    return 0;
}